{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Vite (Chrome)",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/src"
        }
    ]
}
